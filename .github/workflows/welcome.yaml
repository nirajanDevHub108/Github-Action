name: Post Welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write

jobs:
   welcome:
    name: Post Welcome comment
    runs-on: ubuntu-latest

    # ✅ Only run if PR author is a first-time contributor

    if: ${{github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'}}
    steps: 
     - name : Post Welocme Comment
       run: |
         AUTHOR=${{ github.event.pull_request.user.login }}
         gh pr comment "$PR_URL" --body "👋 Welcome @$AUTHOR! 🎉 Thanks for opening your first PR in this repository. We’re excited to have your contribution 🚀 "
        
       env: 
        GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        PR_URL: ${{ github.event.pull_request.html_url}}