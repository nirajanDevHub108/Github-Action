name: 04 - using actions

on:
    push

jobs:
    build:
        runs-on: ubuntu-latest
        #step-0 checkout the code from the repository
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
            - name: printing Folders
              run : ls

        #1.instal deps of our react application

        #execute automated test
